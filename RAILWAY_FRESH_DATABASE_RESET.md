## 📊 ΕΠΙΛΟΓΗ Α: Καθαρισμός Δεδομένων (Προτείνεται)

Αυτή η μέθοδος διαγράφει μόνο τους χρηστές και τα δεδομένα **χωρίς να φτιάξετε νέο database**.

### Βήμα 1: Έχετε Ήδη το Cleanup Script

Το script υπάρχει ήδη στον φάκελο σας:
```
linux_version/backend/scripts/cleanup_all_data.py
```

### Βήμα 2: Εκτέλεση Script στο Railway

**Μέθοδος 1: Μετο Railway Dashboard Shell (Προτείνεται)**

1. Πηγαίνετε στο **Railway Dashboard** → Project σας
2. Κάντε κλικ στο **Backend Service**
3. Κάντε κλικ στο tab **"Shell"**
4. Εκτελέστε:
```bash
python manage.py cleanup_all_data --force
```

**Μέθοδος 2: Με Railway CLI**
```bash
# Connect to Railway
cd linux_version
railway link

# Open shell στο Railway
railway shell

# Στο Railway shell, τρέξτε:
cd /app
python manage.py cleanup_all_data --force
```

**⚠️ ΣΗΜΕΙΩΣΗ:** Το `railway run` εργάζεται στο local environment σας, όχι στο Railway! Για να εκτελέσετε στο Railway, χρησιμοποιήστε το **Shell** από το Railway Dashboard.

### Βήμα 3: Επαναφορά Demo Data

Μετά τη διαγραφή, τρέξτε το auto-initialization:

```bash
# Στο Railway
railway run python3 backend/scripts/auto_initialization.py --force

# Ή:
railway run bash -c "cd backend && python3 scripts/auto_initialization.py --force"
```

**Ή αν τρέχετε στο local Docker:**
```bash
docker exec -it <backend-container-name> python3 scripts/cleanup_all_data.py
docker exec -it <backend-container-name> python3 scripts/auto_initialization.py --force
```

**Ή αν θέλετε να το test νομίσματος locally:**
```bash
# Από τον φάκελο linux_version/
cd backend
python3 scripts/cleanup_all_data.py
python3 scripts/auto_initialization.py --force
```

### Τι Κάνει το Script

Το `cleanup_all_data.py` διαγράφει **σε σωστή σειρά**:
1. ✅ **Οικονομικά δεδομένα** (transactions, payments, expenses, receipts)
2. ✅ **Σχέσεις δαπανών-διαμερισμάτων** (apartment shares, expense apartments)
3. ✅ **Περιόδους κοινών δαπανών** (common expense periods)
4. ✅ **Επαναφορά υπολοίπων διαμερισμάτων** (θέτει 0)
5. ✅ **Ανακοινώσεις, Αιτήματα, Ψηφοφορίες**
6. ✅ **Σχέσεις κτιρίων** (building memberships)
7. ✅ **Κτίρια και Διαμερίσματα**
8. ✅ **Χρήστες** (εκτός superuser)
9. ✅ **Επαναφορά superuser password** (admin / admin123456)

### Παράδειγμα Output

```
======================================================================
  🧹 ΚΑΘΑΡΙΣΜΟΣ ΒΑΣΗΣ ΔΕΔΟΜΕΝΩΝ
======================================================================
📅 Ημερομηνία: 2024-01-15 10:30:45
🏷️  Schema: demo

======================================================================
  📊 ΤΡΕΧΟΝΤΕΣ ΚΑΤΑΣΤΑΣΗ
======================================================================
   👥 Χρήστες: 25
   💰 Συναλλαγές: 150
   💳 Πληρωμές: 45
   💸 Δαπάνες: 30
   🏢 Κτίρια: 3

======================================================================
  ⚠️  ΕΠΙΒΕΒΑΙΩΣΗ
======================================================================
...
✅ Ολοκληρώθηκε η διαγραφή!
```
