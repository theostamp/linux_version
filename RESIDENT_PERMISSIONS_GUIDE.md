# 📋 Οδηγός Δικαιωμάτων Ενοίκων (Resident Permissions Guide)

## 🎯 Σκοπός

Αυτό το έγγραφο περιγράφει **αναλυτικά** τι μπορεί να κάνει ένας **Resident (Ένοικος)** στο σύστημα, τι βλέπει στην οθόνη του, και ποιες λειτουργίες έχει πρόσβαση.

---

## 👤 Τι είναι ο Resident;

Ο **Resident** είναι ένας κάτοικος/ένοικος μιας πολυκατοικίας που:
- **ΔΕΝ** πληρώνει συνδρομή
- **Προσκαλείται** από τον Manager (ιδιοκτήτη γραφείου διαχείρισης)
- Έχει **περιορισμένα** δικαιώματα (read-only για τα περισσότερα)
- Βλέπει **μόνο** τα δεδομένα της δικής του πολυκατοικίας

---

## 🔐 Πώς Δημιουργείται ένας Resident;

### Ροή Πρόσκλησης:

1. **Manager** (ιδιοκτήτης γραφείου):
   - Πληρώνει συνδρομή
   - Δημιουργεί πολυκατοικία
   - Στέλνει **πρόσκληση** σε ένοικο μέσω email

2. **Resident** (ένοικος):
   - Λαμβάνει email πρόσκλησης
   - Κάνει εγγραφή με το invitation link
   - Αυτόματα αποκτά:
     - `role = 'resident'`
     - Membership στο **Resident Group**
     - Σύνδεση με την πολυκατοικία του Manager

---

## 📱 Τι Βλέπει ο Resident στην Οθόνη του;

### 🎨 Header (Κεφαλίδα)

```
Γραφείο Διαχείρισης Κτίριο: [Όνομα Κτιρίου]
john.doe@example.com
Χρήστης  ← Εμφανίζεται ως "Χρήστης" (Resident)
Αποσύνδεση
```

### 🧭 Sidebar Navigation (Μενού)

#### ✅ **Κύρια Λειτουργίες** (Έχει Πρόσβαση)

| Λειτουργία | Icon | Πρόσβαση | Τι Μπορεί να Κάνει |
|-----------|------|----------|-------------------|
| **Πίνακας Ελέγχου** | 🏠 | ✅ READ | Βλέπει overview της πολυκατοικίας |
| **Ανακοινώσεις** | 📢 | ✅ READ | Διαβάζει ανακοινώσεις του Manager |
| **Ψηφοφορίες** | ☑️ | ✅ READ + VOTE | Διαβάζει και **ψηφίζει** |
| **Αιτήματα** | 📋 | ✅ READ + CREATE | Δημιουργεί και βλέπει τα δικά του αιτήματα |
| **Chat** | 💬 | ✅ READ + WRITE | Επικοινωνεί με Manager και άλλους ενοίκους |

#### ❌ **Οικονομικά και Έργα** (ΔΕΝ Έχει Πρόσβαση)

| Λειτουργία | Icon | Πρόσβαση | Λόγος |
|-----------|------|----------|-------|
| **Οικονομικά** | 💶 | ❌ HIDDEN | Μόνο για Managers |
| **Υπηρεσίες & Δαπάνες** | 🔧 | ❌ HIDDEN | Μόνο για Managers |
| **Προσφορές & Έργα** | 📄 | ❌ HIDDEN | Μόνο για Managers |
| **Παραστατικά** | 📄 | ❌ HIDDEN | Μόνο για Managers |

#### ✅ **Προσωπικά** (Έχει Πρόσβαση)

| Λειτουργία | Icon | Πρόσβαση | Τι Μπορεί να Κάνει |
|-----------|------|----------|-------------------|
| **Το Προφίλ μου** | 👤 | ✅ READ + EDIT | Επεξεργάζεται τα στοιχεία του |
| **Η Συνδρομή μου** | 💳 | ✅ READ | Βλέπει ότι είναι Resident (δεν πληρώνει) |

#### ❌ **Kiosk & Display** (ΔΕΝ Έχει Πρόσβαση)

| Λειτουργία | Icon | Πρόσβαση | Λόγος |
|-----------|------|----------|-------|
| **Kiosk Mode** | 📺 | ❌ HIDDEN | Μόνο για Managers |
| **Info Screen** | 📺 | ❌ HIDDEN | Μόνο για Managers |

#### ❌ **Διαχείριση** (ΔΕΝ Έχει Πρόσβαση)

| Λειτουργία | Icon | Πρόσβαση | Λόγος |
|-----------|------|----------|-------|
| **Πολυκατοικίες** | 🏢 | ❌ HIDDEN | Μόνο για Managers |
| **Κάτοικοι** | 👥 | ❌ HIDDEN | Μόνο για Managers |
| **Ειδοποιήσεις** | 📤 | ❌ HIDDEN | Μόνο για Managers |
| **Ρυθμίσεις** | ⚙️ | ❌ HIDDEN | Μόνο για Managers |

---

## 🔍 Αναλυτικά Δικαιώματα ανά Λειτουργία

### 1️⃣ **Πίνακας Ελέγχου (Dashboard)**

**Πρόσβαση:** ✅ **READ-ONLY**

**Τι Βλέπει:**
- 📊 **Στατιστικά Πολυκατοικίας:**
  - Αριθμός διαμερισμάτων
  - Ενεργές ανακοινώσεις
  - Εκκρεμείς ψηφοφορίες
  - Αιτήματα σε εξέλιξη

- 📢 **Πρόσφατες Ανακοινώσεις:**
  - Τίτλος
  - Περίληψη
  - Ημερομηνία

- ☑️ **Ενεργές Ψηφοφορίες:**
  - Θέμα ψηφοφορίας
  - Deadline
  - Κουμπί "Ψήφισε" (αν δεν έχει ψηφίσει)

**Τι ΔΕΝ Βλέπει:**
- ❌ Οικονομικά στοιχεία (έσοδα, έξοδα, υπόλοιπα)
- ❌ Προσωπικά στοιχεία άλλων ενοίκων
- ❌ Διαχειριστικά controls

---

### 2️⃣ **Ανακοινώσεις (Announcements)**

**Πρόσβαση:** ✅ **READ-ONLY**

**Τι Μπορεί να Κάνει:**
- ✅ Διαβάζει όλες τις ανακοινώσεις της πολυκατοικίας του
- ✅ Βλέπει:
  - Τίτλο
  - Περιεχόμενο
  - Ημερομηνία δημοσίευσης
  - Συνημμένα αρχεία (αν υπάρχουν)
- ✅ Φιλτράρει ανακοινώσεις (πρόσφατες, παλιές)

**Τι ΔΕΝ Μπορεί να Κάνει:**
- ❌ Δημιουργία νέας ανακοίνωσης
- ❌ Επεξεργασία υπάρχουσας ανακοίνωσης
- ❌ Διαγραφή ανακοίνωσης
- ❌ Upload συνημμένων

**Backend Permission:** `ExpensePermission` → `SAFE_METHODS` (GET)

---

### 3️⃣ **Ψηφοφορίες (Votes)**

**Πρόσβαση:** ✅ **READ + VOTE**

**Τι Μπορεί να Κάνει:**
- ✅ Βλέπει όλες τις ψηφοφορίες της πολυκατοικίας του
- ✅ **Ψηφίζει** σε ενεργές ψηφοφορίες
- ✅ Βλέπει αποτελέσματα (μετά το κλείσιμο)
- ✅ Βλέπει:
  - Θέμα ψηφοφορίας
  - Περιγραφή
  - Επιλογές
  - Deadline
  - Αποτελέσματα (αν έχει κλείσει)

**Τι ΔΕΝ Μπορεί να Κάνει:**
- ❌ Δημιουργία νέας ψηφοφορίας
- ❌ Επεξεργασία ψηφοφορίας
- ❌ Διαγραφή ψηφοφορίας
- ❌ Κλείσιμο ψηφοφορίας πριν το deadline
- ❌ Αλλαγή της ψήφου του (μετά την υποβολή)

**Περιορισμοί:**
- Μπορεί να ψηφίσει **μόνο μία φορά** ανά ψηφοφορία
- Βλέπει **μόνο** τις ψηφοφορίες της δικής του πολυκατοικίας

---

### 4️⃣ **Αιτήματα (Requests)**

**Πρόσβαση:** ✅ **READ + CREATE**

**Τι Μπορεί να Κάνει:**
- ✅ **Δημιουργεί** νέα αιτήματα (π.χ. βλάβες, παράπονα)
- ✅ Βλέπει **τα δικά του** αιτήματα
- ✅ Βλέπει την κατάσταση των αιτημάτων του:
  - Εκκρεμεί
  - Σε εξέλιξη
  - Ολοκληρώθηκε
- ✅ Προσθέτει σχόλια στα δικά του αιτήματα
- ✅ Upload φωτογραφίες/αρχεία

**Τι ΔΕΝ Μπορεί να Κάνει:**
- ❌ Βλέπει αιτήματα **άλλων ενοίκων**
- ❌ Αλλάζει την κατάσταση του αιτήματος (μόνο ο Manager)
- ❌ Διαγράφει αιτήματα
- ❌ Αναθέτει αιτήματα σε τρίτους

**Backend Permission:** Custom logic - βλέπει μόνο τα δικά του

---

### 5️⃣ **Chat**

**Πρόσβαση:** ✅ **READ + WRITE**

**Τι Μπορεί να Κάνει:**
- ✅ Στέλνει μηνύματα στον **Manager**
- ✅ Στέλνει μηνύματα σε **άλλους ενοίκους** (αν επιτρέπεται)
- ✅ Βλέπει το ιστορικό συνομιλιών
- ✅ Λαμβάνει ειδοποιήσεις για νέα μηνύματα
- ✅ Upload αρχεία/εικόνες

**Τι ΔΕΝ Μπορεί να Κάνει:**
- ❌ Δημιουργεί group chats (μόνο ο Manager)
- ❌ Διαγράφει μηνύματα άλλων
- ❌ Αλλάζει ρυθμίσεις chat

**Περιορισμοί:**
- Βλέπει **μόνο** τις συνομιλίες της δικής του πολυκατοικίας

---

### 6️⃣ **Οικονομικά (Financial Management)**

**Πρόσβαση:** ❌ **DENIED**

**Τι Συμβαίνει:**
- Το menu item **ΔΕΝ εμφανίζεται** στο sidebar
- Αν προσπαθήσει να μπει με URL (`/financial`):
  - Εμφανίζεται: **"Μη Εξουσιοδοτημένη Πρόσβαση"**
  - Μήνυμα: "Δεν έχετε τα απαραίτητα δικαιώματα για να δείτε αυτή τη σελίδα."

**Λόγος:**
- Τα οικονομικά είναι **ευαίσθητα δεδομένα**
- Μόνο οι **Managers** (που πληρώνουν συνδρομή) έχουν πρόσβαση

**Backend Permission:** 
```python
# financial/permissions.py
ExpensePermission → SAFE_METHODS
  - Managers: ✅ True
  - Residents: ❌ False (για financial module)
```

**Frontend Permission:**
```typescript
// useFinancialPermissions.ts
financial_read: role === 'manager' || role === 'superuser'
  - Resident: ❌ False
```

---

### 7️⃣ **Το Προφίλ μου (My Profile)**

**Πρόσβαση:** ✅ **READ + EDIT**

**Τι Μπορεί να Κάνει:**
- ✅ Επεξεργάζεται:
  - Όνομα
  - Email
  - Τηλέφωνο
  - Διεύθυνση
  - Φωτογραφία προφίλ
- ✅ Αλλάζει κωδικό
- ✅ Βλέπει τα στοιχεία του διαμερίσματός του

**Τι ΔΕΝ Μπορεί να Κάνει:**
- ❌ Αλλάζει το role του
- ❌ Αλλάζει την πολυκατοικία του
- ❌ Διαγράφει το account του (πρέπει να ζητήσει από Manager)

---

### 8️⃣ **Η Συνδρομή μου (My Subscription)**

**Πρόσβαση:** ✅ **READ-ONLY**

**Τι Βλέπει:**
- ✅ **Status:** "Resident - Δεν απαιτείται συνδρομή"
- ✅ Μήνυμα: "Έχετε προσκληθεί από τον Manager της πολυκατοικίας σας"
- ✅ Στοιχεία Manager (όνομα, email)

**Τι ΔΕΝ Βλέπει:**
- ❌ Πληρωμές
- ❌ Τιμολόγια
- ❌ Upgrade options (δεν μπορεί να γίνει Manager)

---

## 🔒 Backend Permissions (Τεχνικά)

### Django Groups

```python
# Resident Group Membership
user.groups.filter(name='Resident').exists()  # ✅ True
user.groups.filter(name='Manager').exists()   # ❌ False
```

### Permission Classes

#### 1. **ExpensePermission**
```python
# SAFE_METHODS (GET, HEAD, OPTIONS)
if request.method in permissions.SAFE_METHODS:
    # Residents: ✅ Μπορούν να διαβάζουν δαπάνες
    return user.groups.filter(name='Resident').exists()

# POST, PUT, PATCH, DELETE
else:
    # Residents: ❌ ΔΕΝ μπορούν να δημιουργούν/επεξεργάζονται
    return user.groups.filter(name='Manager').exists()
```

#### 2. **PaymentPermission**
```python
# SAFE_METHODS (GET, HEAD, OPTIONS)
if request.method in permissions.SAFE_METHODS:
    # Residents: ✅ Μπορούν να διαβάζουν πληρωμές
    return user.groups.filter(name='Resident').exists()

# POST, PUT, PATCH, DELETE
else:
    # Residents: ❌ ΔΕΝ μπορούν να δημιουργούν/επεξεργάζονται
    return user.groups.filter(name='Manager').exists()
```

#### 3. **IsRelatedToBuilding**
```python
# Residents βλέπουν ΜΟΝΟ τα δεδομένα της δικής τους πολυκατοικίας
def has_object_permission(self, request, view, obj):
    building = getattr(obj, 'building', None)
    if building:
        # Έλεγχος αν ο resident ανήκει σε αυτή την πολυκατοικία
        return user.building == building
    return False
```

---

## 🎨 Frontend Permissions (Τεχνικά)

### Role Check in Sidebar

```typescript
// Sidebar.tsx
const navigationGroups: NavigationGroup[] = [
  {
    id: 'operations',
    title: 'Οικονομικά και Έργα',
    links: [
      {
        href: '/financial',
        label: 'Οικονομικά',
        roles: ['manager', 'staff', 'superuser'], // ❌ Resident ΔΕΝ περιλαμβάνεται
      },
    ]
  },
  {
    id: 'main',
    title: 'Κύρια Λειτουργίες',
    links: [
      {
        href: '/dashboard',
        label: 'Πίνακας Ελέγχου',
        roles: ['manager', 'resident', 'staff', 'superuser'], // ✅ Resident περιλαμβάνεται
      },
    ]
  },
];

// Φιλτράρισμα links βάσει role
const filteredLinks = navigationGroups
  .map(group => ({
    ...group,
    links: group.links.filter(link => link.roles.includes(userRole))
  }))
  .filter(group => group.links.length > 0);
```

### Financial Permissions Hook

```typescript
// useFinancialPermissions.ts
export function useFinancialPermissions() {
  const { user } = useAuth();
  const role = user.profile?.role;

  switch (permission) {
    case 'financial_read':
      // Residents: ❌ ΔΕΝ έχουν πρόσβαση
      return role === 'manager' || role === 'superuser';

    case 'financial_write':
      // Residents: ❌ ΔΕΝ έχουν πρόσβαση
      return role === 'manager' || role === 'superuser';

    // ... άλλα permissions
  }
}
```

### Protected Routes

```typescript
// ProtectedFinancialRoute.tsx
<ProtectedFinancialRoute requiredPermission="financial_read">
  <FinancialContent />
</ProtectedFinancialRoute>

// Αν ο Resident προσπαθήσει να μπει:
// → Εμφανίζεται "Μη Εξουσιοδοτημένη Πρόσβαση"
```

---

## 📊 Σύγκριση: Manager vs Resident

| Λειτουργία | Manager | Resident |
|-----------|---------|----------|
| **Πληρωμή Συνδρομής** | ✅ Ναι | ❌ Όχι |
| **Πίνακας Ελέγχου** | ✅ Full Access | ✅ Read-Only |
| **Ανακοινώσεις** | ✅ Create/Edit/Delete | ✅ Read-Only |
| **Ψηφοφορίες** | ✅ Create/Edit/Delete | ✅ Read + Vote |
| **Αιτήματα** | ✅ View All + Manage | ✅ Create + View Own |
| **Chat** | ✅ Full Access | ✅ Full Access |
| **Οικονομικά** | ✅ Full Access | ❌ No Access |
| **Δαπάνες** | ✅ Create/Edit/Delete | ❌ No Access |
| **Έργα** | ✅ Create/Edit/Delete | ❌ No Access |
| **Παραστατικά** | ✅ Upload/View | ❌ No Access |
| **Πολυκατοικίες** | ✅ Manage | ❌ No Access |
| **Κάτοικοι** | ✅ Invite/Manage | ❌ No Access |
| **Kiosk Mode** | ✅ Configure | ❌ No Access |
| **Ρυθμίσεις** | ✅ Full Access | ❌ No Access |

---

## 🚀 Πώς να Τεστάρεις τα Resident Permissions

### 1. Δημιουργία Test Resident

```bash
# Backend
python manage.py shell

from users.models import CustomUser
from django.contrib.auth.models import Group

# Δημιουργία resident
resident = CustomUser.objects.create_user(
    email='resident@test.com',
    password='test123',
    first_name='John',
    last_name='Doe',
    role='resident'
)

# Προσθήκη στο Resident Group
resident_group = Group.objects.get(name='Resident')
resident.groups.add(resident_group)

print(f"Resident created: {resident.email}")
print(f"Role: {resident.role}")
print(f"Groups: {[g.name for g in resident.groups.all()]}")
```

### 2. Login ως Resident

```
Email: resident@test.com
Password: test123
```

### 3. Έλεγχος Πρόσβασης

#### ✅ Πρέπει να Δει:
- Dashboard
- Ανακοινώσεις
- Ψηφοφορίες
- Αιτήματα
- Chat
- Το Προφίλ μου
- Η Συνδρομή μου

#### ❌ ΔΕΝ Πρέπει να Δει:
- Οικονομικά
- Υπηρεσίες & Δαπάνες
- Προσφορές & Έργα
- Παραστατικά
- Πολυκατοικίες
- Κάτοικοι
- Ειδοποιήσεις
- Kiosk Mode
- Ρυθμίσεις

#### 🧪 Τεστ URL Access:
```bash
# Αυτά πρέπει να δουλεύουν
/dashboard          # ✅ 200 OK
/announcements      # ✅ 200 OK
/votes              # ✅ 200 OK
/requests           # ✅ 200 OK
/chat               # ✅ 200 OK

# Αυτά πρέπει να δείχνουν "Unauthorized"
/financial          # ❌ 403 Forbidden
/maintenance        # ❌ 403 Forbidden
/projects           # ❌ 403 Forbidden
/buildings          # ❌ 403 Forbidden
/residents          # ❌ 403 Forbidden
```

---

## 🔧 Troubleshooting

### Πρόβλημα: Resident βλέπει Οικονομικά

**Αιτία:** Δεν έχει οριστεί σωστά το Group

**Λύση:**
```bash
python manage.py shell

from users.models import CustomUser
from django.contrib.auth.models import Group

user = CustomUser.objects.get(email='resident@test.com')

# Αφαίρεση από Manager Group
if user.groups.filter(name='Manager').exists():
    manager_group = Group.objects.get(name='Manager')
    user.groups.remove(manager_group)

# Προσθήκη στο Resident Group
resident_group, _ = Group.objects.get_or_create(name='Resident')
user.groups.add(resident_group)

# Ορισμός role
user.role = 'resident'
user.is_staff = False
user.is_superuser = False
user.save()

print(f"Fixed: {user.email}")
print(f"Groups: {[g.name for g in user.groups.all()]}")
```

### Πρόβλημα: Resident ΔΕΝ βλέπει Dashboard

**Αιτία:** Δεν έχει συνδεθεί με πολυκατοικία

**Λύση:**
```bash
python manage.py shell

from users.models import CustomUser
from buildings.models import Building

user = CustomUser.objects.get(email='resident@test.com')
building = Building.objects.first()  # Ή συγκεκριμένη πολυκατοικία

# Σύνδεση με πολυκατοικία
user.building = building
user.save()

print(f"User {user.email} linked to building {building.name}")
```

---

## 📚 Σχετικά Αρχεία

### Backend:
- `backend/financial/permissions.py` - Permission classes
- `backend/core/permissions.py` - Base permissions (IsResident, IsRelatedToBuilding)
- `backend/users/models.py` - CustomUser model
- `backend/buildings/models.py` - Building model

### Frontend:
- `frontend/components/Sidebar.tsx` - Navigation με role filtering
- `frontend/hooks/useFinancialPermissions.ts` - Financial permissions hook
- `frontend/components/financial/ProtectedFinancialRoute.tsx` - Route protection
- `frontend/types/kiosk/index.ts` - Role definitions και ROLE_PERMISSIONS

### Documentation:
- `USER_ROLE_FIX.md` - Πώς να διορθώσεις user roles
- `WEBHOOK_ROLE_FIX.md` - Πώς λειτουργεί το role assignment στο webhook
- `FINANCIAL_ACCESS_FIX.md` - Πώς να διορθώσεις financial access issues

---

## 💡 Best Practices

### Για Developers:

1. **Πάντα έλεγχε το Group membership:**
   ```python
   user.groups.filter(name='Resident').exists()
   ```

2. **Χρησιμοποίησε τα built-in permission classes:**
   ```python
   from financial.permissions import ExpensePermission
   ```

3. **Φιλτράρε queries βάσει building:**
   ```python
   expenses = Expense.objects.filter(building=user.building)
   ```

### Για Managers:

1. **Πρόσκληση Residents:**
   - Χρησιμοποίησε το invitation system
   - Μην δίνεις Manager access σε residents

2. **Έλεγχος Πρόσβασης:**
   - Residents βλέπουν μόνο τα δεδομένα της πολυκατοικίας τους
   - Οικονομικά είναι **πάντα** private

---

## 🎯 Συμπέρασμα

Ο **Resident** έχει:
- ✅ **Πρόσβαση** σε βασικές λειτουργίες (ανακοινώσεις, ψηφοφορίες, αιτήματα, chat)
- ✅ **Δυνατότητα** να ψηφίζει και να δημιουργεί αιτήματα
- ❌ **Καμία πρόσβαση** σε οικονομικά, διαχείριση, και διαχειριστικές λειτουργίες

Αυτό διασφαλίζει:
- 🔒 **Ασφάλεια:** Ευαίσθητα δεδομένα προστατεύονται
- 🎯 **Απλότητα:** Residents βλέπουν μόνο ό,τι χρειάζονται
- 💰 **Monetization:** Μόνο οι Managers (που πληρώνουν) έχουν πλήρη πρόσβαση


