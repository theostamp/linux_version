'use client';

import * as React from 'react';
import { Card } from './card';

export { CardContent, CardHeader, CardTitle, CardFooter, CardDescription } from './card';

const AppCard = Card;

export { AppCard };
