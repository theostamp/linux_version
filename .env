# .env
# ──────────────────────────────
# 📦  PostgreSQL
# ──────────────────────────────
POSTGRES_DB=new_concierge_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ──────────────────────────────
# 🐍  Django / περιβάλλον
# ──────────────────────────────
DJANGO_SECRET_KEY=please-change-me
ENV=development          # ή production
DJANGO_DEBUG=True        # False σε production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# django-tenants
PUBLIC_DOMAIN=localhost

# ──────────────────────────────
# 👑  Superuser (αν θες auto-create)
# ──────────────────────────────
DJANGO_SUPERUSER_EMAIL=theostam1966@gmail.com
DJANGO_SUPERUSER_FIRST_NAME=theo
DJANGO_SUPERUSER_LAST_NAME=theo

# ──────────────────────────────
# 📂  Static / Media (volume dirs)
# ──────────────────────────────
STATIC_ROOT=/vol/static
MEDIA_ROOT=/vol/media

# ──────────────────────────────
# 🌐  CORS / CSRF
# ──────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://athinon12.localhost:3000
CORS_ALLOWED_ORIGIN_REGEXES=^http://[\\w\\-]+\\.localhost:3000$
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://athinon12.localhost:3000
CORS_ALLOW_HEADERS=accept,accept-encoding,authorization,content-type,dnt,origin,user-agent,x-csrftoken,x-requested-with,x-xsrf-token
CORS_ALLOW_CREDENTIALS=True
CSRF_COOKIE_SAMESITE=Lax

# ──────────────────────────────
# 🐳  Docker flag (προαιρετικό)
# ──────────────────────────────
DOCKER=True

# ──────────────────────────────
# ✉️  SMTP (αν στείλεις email)
# ──────────────────────────────
EMAIL_HOST_USER=you@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=noreply@localhost

# ──────────────────────────────
# 🖥️  Frontend → API base URL
# ──────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000/api
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,neo.localhost

# .env (ΜΗΝ το ανεβάσεις στο Git!)
DJANGO_SECRET_KEY="s0m3super3cr3tvalu3"

DJANGO_SUPERUSER_PASSWORD=admin123