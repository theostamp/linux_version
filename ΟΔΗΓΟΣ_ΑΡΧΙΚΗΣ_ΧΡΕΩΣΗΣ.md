# 📅 Οδηγός Αρχικής Χρέωσης Συστήματος

**Ημερομηνία:** 10 Οκτωβρίου 2025  
**Σημαντικότητα:** 🚨 ΚΡΙΣΙΜΟ

---

## 🎯 Σκοπός

Αυτός ο οδηγός εξηγεί πώς να ορίσετε σωστά την **αρχική ημερομηνία χρέωσης** του συστήματος, ώστε:
- ✅ Να μην χρεώνονται management fees από το -άπειρο
- ✅ Να μην χρεώνεται reserve fund από το -άπειρο
- ✅ Να έχει σαφές σημείο εκκίνησης το σύστημα

---

## ⚠️ Το Πρόβλημα

Χωρίς σαφή αρχική ημερομηνία, το σύστημα θα:
- ❌ Υπολογίζει management fees από την παλαιότερη δαπάνη
- ❌ Συσσωρεύει χρεώσεις από το παρελθόν
- ❌ Εμφανίζει τεράστια ποσά στα "Previous Obligations"

**Παράδειγμα:**
```
Αν υπάρχει δαπάνη από 2020 και ξεκινάτε το σύστημα το 2025:
- Management fees: 10€/μήνα × 60 μήνες = 600€ ΑΠΟ ΤΟ ΠΟΥΘΕΝΑ! ❌
```

---

## ✅ Η Λύση

### Βήμα 1: Ορισμός `financial_system_start_date`

Κατά τη δημιουργία νέου κτιρίου, ορίστε **ΠΑΝΤΑ** το `financial_system_start_date`:

```python
# Στο Building model
building = Building.objects.create(
    name="Πολυκατοικία Α",
    financial_system_start_date=date(2025, 10, 1),  # ✅ Η ημερομηνία που ΞΕΚΙΝΑ το σύστημα
    management_fee_per_apartment=Decimal('10.00'),
    ...
)
```

### Βήμα 2: Τι Σημαίνει Αυτό;

Με `financial_system_start_date = 2025-10-01`:

| Μήνας | Management Fees | Reserve Fund | Previous Obligations |
|-------|----------------|--------------|----------------------|
| Σεπτέμβριος 2025 | €0 ⛔ | €0 ⛔ | €0 ⛔ |
| Οκτώβριος 2025 | €10 ✅ | €50 ✅ | €0 (πρώτος μήνας) |
| Νοέμβριος 2025 | €10 ✅ | €50 ✅ | €60 (από Οκτώβριο) ✅ |

**Κανόνας:** Το σύστημα χρεώνει **ΜΟΝΟ** από την ημερομηνία έναρξης και μετά.

---

## 🔧 Πώς Λειτουργεί Τεχνικά

### 1. BalanceCalculationService

```python
# Αρχείο: financial/balance_service.py
system_start_date = building.financial_system_start_date

if system_start_date is None:
    # ⚠️ FALLBACK: Χρησιμοποιεί την παλαιότερη δαπάνη
    oldest_expense = Expense.objects.filter(...).order_by('date').first()
    system_start_date = oldest_expense.date.replace(day=1)

# Υπολογισμός χρεώσεων ΜΟΝΟ από system_start_date
management_fee_transactions = Transaction.objects.filter(
    apartment=apartment,
    type='management_fee_charge',
    date__gte=system_start_date,  # ✅ ΟΧΙ ΠΡΙΝ!
    date__lt=month_start
)
```

### 2. MonthlyBalanceService

```python
# Αρχείο: financial/monthly_balance_service.py

def _calculate_management_fees(self, year: int, month: int) -> Decimal:
    month_start = date(year, month, 1)
    
    # ✅ ΕΛΕΓΧΟΣ: Αν ο μήνας είναι πριν το system start date
    if self.building.financial_system_start_date:
        if month_start < self.building.financial_system_start_date:
            return Decimal('0.00')  # ⛔ ΔΕΝ ΧΡΕΩΝΟΥΜΕ!
    
    # Χρέωση management fees για αυτόν το μήνα
    ...
```

---

## 📋 Checklist Αρχικοποίησης

Όταν δημιουργείτε νέο κτίριο:

- [ ] Ορίστε `financial_system_start_date` (συνήθως 1η του μήνα)
- [ ] Ορίστε `management_fee_per_apartment` (αν υπάρχει)
- [ ] Ορίστε reserve fund settings (αν υπάρχει)
- [ ] Δημιουργήστε τα διαμερίσματα
- [ ] Τρέξτε `create_monthly_charges` για τον πρώτο μήνα

```bash
# Παράδειγμα
python manage.py create_monthly_charges --building 1 --month 2025-10
```

---

## 🧪 Πώς να Επιβεβαιώσετε

### Test 1: Έλεγχος Αρχικού Μήνα

```bash
python manage.py fix_balance_carryover --building 1 --verify-only --from 2025-10
```

Θα δείτε:
```
✅ 10/2025: 
   Management Fees: €80.00 (8 διαμερίσματα × €10)
   Previous Obligations: €0.00 (πρώτος μήνας)
```

### Test 2: Έλεγχος Μηνών Πριν την Έναρξη

```bash
python manage.py shell
>>> from financial.monthly_balance_service import MonthlyBalanceService
>>> from buildings.models import Building
>>> building = Building.objects.get(id=1)
>>> service = MonthlyBalanceService(building)
>>> 
>>> # Δοκιμή μήνα ΠΡΙΝ την έναρξη
>>> fees = service._calculate_management_fees(2025, 9)  # Σεπτέμβριος
>>> print(fees)
0.00  # ✅ ΣΩΣΤΟ! Δεν χρεώνει πριν την έναρξη
>>> 
>>> # Δοκιμή μήνα ΜΕΤΑ την έναρξη
>>> fees = service._calculate_management_fees(2025, 10)  # Οκτώβριος
>>> print(fees)
80.00  # ✅ ΣΩΣΤΟ! Χρεώνει από την έναρξη
```

---

## 🎯 Βέλτιστες Πρακτικές

### 1. Νέα Πολυκατοικία (Χωρίς Ιστορικό)

```python
building = Building.objects.create(
    name="Νέα Πολυκατοικία",
    financial_system_start_date=date.today().replace(day=1),  # 1η του τρέχοντος μήνα
    ...
)
```

### 2. Υπάρχουσα Πολυκατοικία (Με Ιστορικό)

Αν έχετε ιστορικές δαπάνες αλλά ξεκινάτε το management fee σύστημα τώρα:

```python
building = Building.objects.create(
    name="Υπάρχουσα Πολυκατοικία",
    financial_system_start_date=date(2025, 10, 1),  # Ημερομηνία έναρξης management fees
    ...
)

# Οι παλιές δαπάνες (π.χ. από 2024) θα υπάρχουν στο σύστημα
# ΑΛΛΑ τα management fees θα χρεώνονται ΜΟΝΟ από 10/2025 ✅
```

### 3. Migration Από Παλιό Σύστημα

```python
# Αν το κτίριο υπάρχει ήδη χωρίς financial_system_start_date
building = Building.objects.get(id=1)
building.financial_system_start_date = date(2025, 10, 1)
building.save()

# Επανυπολογισμός όλων των μηνών
python manage.py fix_balance_carryover --building 1 --from 2025-10
```

---

## ⚠️ Τι ΝΑ ΑΠΟΦΥΓΕΤΕ

### ❌ ΜΗΝ:
- Αφήνετε το `financial_system_start_date` κενό σε νέα κτίρια
- Ορίζετε πολύ παλιά ημερομηνία (π.χ. 2020) εκτός αν έχετε πραγματικά δεδομένα από τότε
- Αλλάζετε το `financial_system_start_date` αφού έχουν δημιουργηθεί χρεώσεις

### ✅ ΝΑ:
- Ορίζετε ΠΑΝΤΑ το `financial_system_start_date` για νέα κτίρια
- Χρησιμοποιείτε την 1η του μήνα ως ημερομηνία έναρξης
- Επαληθεύετε με το management command μετά την αρχικοποίηση

---

## 📊 UI/UX Συστάσεις

Στο frontend, όταν δημιουργείται νέο κτίριο:

1. **Υποχρεωτικό πεδίο:** "Ημερομηνία Έναρξης Οικονομικού Συστήματος"
2. **Default value:** 1η του τρέχοντος μήνα
3. **Επεξήγηση:** "Από ποια ημερομηνία θα χρεώνονται management fees και reserve fund"
4. **Προειδοποίηση:** "Οι χρεώσεις θα ξεκινήσουν από αυτή την ημερομηνία και μετά"

```tsx
<DatePicker
  label="Ημερομηνία Έναρξης Συστήματος"
  value={financialSystemStartDate}
  onChange={setFinancialSystemStartDate}
  required
  helperText="Από ποια ημερομηνία θα χρεώνονται τα management fees"
  defaultValue={startOfMonth(new Date())}
/>
```

---

## 🎉 Τελικό Checklist

Πριν το production:

- [x] ✅ BalanceCalculationService ελέγχει `financial_system_start_date`
- [x] ✅ MonthlyBalanceService._calculate_management_fees() ελέγχει start date
- [x] ✅ MonthlyBalanceService._calculate_reserve_fund() ελέγχει start date
- [x] ✅ Fallback σε oldest expense αν δεν υπάρχει start date
- [x] ✅ Documentation για developers
- [ ] ⏳ UI validation για required field
- [ ] ⏳ Frontend helper text
- [ ] ⏳ Onboarding wizard για νέα κτίρια

---

**Κατάσταση:** ✅ BACKEND ΕΤΟΙΜΟ  
**Επόμενο Βήμα:** Frontend integration

**Δημιουργήθηκε:** 10 Οκτωβρίου 2025  
**Τελευταία Ενημέρωση:** 10 Οκτωβρίου 2025


